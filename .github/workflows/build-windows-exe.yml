name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'host_visual.py'
      - 'requirements-host.txt'
      - 'build_host.bat'
      - '.github/workflows/build-windows-exe.yml'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        shell: cmd
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-host.txt
          python -m pip install pyinstaller

      - name: Syntax check
        shell: cmd
        run: python -m py_compile host_visual.py

      - name: Build EXE
        shell: cmd
        run: pyinstaller --noconfirm --clean --onefile --windowed --uac-admin --name MusicVMAutoVisual host_visual.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: MusicVMAutoVisual-Windows
          path: dist/MusicVMAutoVisual.exe
          if-no-files-found: error
